HOST=0.0.0.0
PORT=1337

# Security Keys
APP_KEYS="toBeModified1,toBeModified2"
API_TOKEN_SALT=tobemodified
ADMIN_JWT_SECRET=tobemodified
TRANSFER_TOKEN_SALT=tobemodified
JWT_SECRET=tobemodified
CSRF_SECRET=tobemodified

# CORS Configuration
CORS_ORIGINS=http://localhost:3000,http://localhost:5173,http://localhost:4200,http://localhost:8080

# JWT Configuration
JWT_EXPIRATION=30d # 30 days
JWT_REFRESH_EXPIRATION=90d # 90 days

# Rate Limiting
RATE_LIMIT_WINDOW=15 # 15 minutes
RATE_LIMIT_MAX=100 # 100 requests per window

# Database Configuration
DATABASE_CLIENT=sqlite
DATABASE_FILENAME=.tmp/data.db

# Optional: Email Configuration (if needed later)
# SMTP_HOST=smtp.example.com
# SMTP_PORT=587
# SMTP_USERNAME=your_username
# SMTP_PASSWORD=your_password
# SMTP_FROM=noreply@example.com
# SMTP_REPLY_TO=support@example.com
